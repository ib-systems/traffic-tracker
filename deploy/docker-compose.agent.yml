services:
  ovn-traffic-agent:
    image: ovn-traffic-agent:latest
    container_name: ovn-traffic-agent
    command:
      - --config
      - /etc/ovn-traffic-agent/agent.yml
    volumes:
      - ./agent.yml:/etc/ovn-traffic-agent/agent.yml:ro
    network_mode: host
    restart: unless-stopped
